spring:
  application:
    name: ai-gateway-service
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: gpt-4o-mini

tools:
  allowlist:
    - runbook-search
    - sql-analytics
    - fhir-validation
